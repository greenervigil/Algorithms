function boo(bool) {
  // What is the new fad diet for ghost developers? The Boolean.
  if(bool === true) {
        bool = true;
    } else if (bool === false) {
        bool = true;
    } else {
        bool = false;
    }
    return bool;
}

boo(null);
